#!/bin/bash

set -u
set -e

for project in "isitketo" "zestful"
do
  CSV_URL_FILE="${project}-csv-url.txt"
  CSV_URL=$(cat $CSV_URL_FILE)
  echo "Table for ${project}"
  wget -qO- "$CSV_URL" | ./app/main.py 2> /dev/null
done